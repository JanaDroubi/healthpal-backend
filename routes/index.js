const express = require('express');
const router = express.Router();

// Mount individual routers here when ready, e.g.
// router.use('/patients', require('./patientRoutes'));

module.exports = router;
