# HealthPal Backend (scaffold)

This is a generated scaffold for the HealthPal backend using Express, Sequelize and MySQL.

Setup:

1. Copy `.env.example` to `.env` and set your values.
2. npm install
3. npm run dev

The server exposes a simple GET / route.
